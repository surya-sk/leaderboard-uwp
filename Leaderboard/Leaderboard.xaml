<Page
    x:Class="Leaderboard.Leaderboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leaderboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/> 
        </Grid.RowDefinitions>
        
        <TextBlock Text="Player(s)" FontSize="50"/>
        <TextBlock Text="Score(s)" FontSize="50" Grid.Column="1"/>

        <StackPanel Orientation="Vertical" MaxWidth="300" Grid.Row="1">
            <TextBox PlaceholderText="Player name" />
            <TextBox PlaceholderText="Player name" />
            <TextBox PlaceholderText="Player name" />
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="1">
            <TextBox PlaceholderText="Player score" />
            <TextBox PlaceholderText="Player score" />
            <TextBox PlaceholderText="Player score" />
        </StackPanel>
    </Grid>
</Page>
