<Page
    x:Class="Leaderboard.Leaderboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Leaderboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Player" FontSize="36" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="Score" FontSize="36" HorizontalAlignment="Center"/>

        <RelativePanel Grid.Column="0" Grid.Row="1">
            <TextBox PlaceholderText="Add Player" FontSize="36"/>
        </RelativePanel>
        <RelativePanel Grid.Column="1" Grid.Row="1">
            <TextBox PlaceholderText="Add Score" FontSize="36"/>
        </RelativePanel>
    </Grid>
</Page>
